server {
    listen 8080;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This handles React Routing correctly
        try_files $uri $uri/ /index.html;
    }
}
