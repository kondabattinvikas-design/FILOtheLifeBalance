server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line is important for React routing
        try_files $uri $uri/ /index.html;
    }
}
